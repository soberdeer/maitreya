import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    marginBottom: 20,
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 10,
  },
});
