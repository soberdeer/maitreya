import { createUseStyles } from 'react-jss';

export default createUseStyles({
  flex: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    cursor: 'pointer',
    marginBottom: 10,
    gap: 10,
  },
});
