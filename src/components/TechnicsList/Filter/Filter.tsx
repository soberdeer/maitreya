// import React from 'react';
// import { Menu, UnstyledButton } from '@mantine/core';
// import { Filter as FilterIcon, Check } from 'react-feather';
// import { TechnicStateType } from '@src/util/types';

// import useStyles from './Filter.styles';

// export interface FilterProps extends Omit<React.HTMLProps<HTMLDivElement>, 'onSelect'> {
//   value: string;
//
//   onSelect(value: TechnicStateType): void;
// }

// const states = ['Все', 'В фуге', 'Вне фуги'];

export function Filter() {
  // const { classes, cx } = useStyles();

  return null;
  // <div className={cx(classes.root, className)} {...others}>
  //   <Menu width={210}>
  //     <Menu.Target>
  //       <UnstyledButton className={classes.filterButton}>
  //           <FilterIcon size={20} color={colors.secondary} />
  //       </UnstyledButton>
  //     </Menu.Target>
  //     <Menu.Dropdown className={classes.dropdown}>
  //       {states.map((item: TechnicStateType, index) => (
  //         <Menu.Item
  //           icon={item === value && <Check size={14} />}
  //           className={classes.item}
  //           key={index}
  //           onClick={() => onSelect(item)}
  //         >
  //           {item}
  //         </Menu.Item>
  //       ))}
  //     </Menu.Dropdown>
  //   </Menu>
  // </div>
}
