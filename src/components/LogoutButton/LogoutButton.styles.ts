import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  root: {
    padding: 0,
    margin: 0,
    backgroundColor: 'transparent',
    width: 20,
    height: 20,
    cursor: 'pointer',
  },
}));
