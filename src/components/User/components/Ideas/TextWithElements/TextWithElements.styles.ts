import { createStyles } from '@mantine/core';

export default createStyles({
  root: {
    marginBottom: 20,
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 10,
  },
});
