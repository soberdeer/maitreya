import { createStyles } from '@mantine/core';

export default createStyles({
  root: {
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 10,
  },

  text: {
    marginBlockEnd: 0,
    fontSize: 20,
    textShadow: 'none',
    fontFamily: 'Arounder',
  },
});
